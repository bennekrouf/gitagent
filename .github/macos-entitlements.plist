<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Required for wry/WebKit — JIT and dynamic library loading. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Outbound network: the LLM provider (Ollama / DeepSeek) and the forge
       CLIs (git, gh) reaching GitHub / Azure DevOps. -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- User-selected file access: rfd folder/file pickers (choosing a
       workspace, browsing to an SSH identity file). -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
